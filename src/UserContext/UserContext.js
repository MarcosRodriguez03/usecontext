import { createContext } from "react";

// create context creates a new object amd we are storing that insidae of our usercontext varibale
const UserContext = createContext();

export default UserContext